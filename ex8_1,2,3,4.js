var a = 1; b = 2; c = 1;

if (a !== 0 ) {
  
  d = b ** 2 - 4 * a * c
  
  x = (- b + Math.sqrt(d)) / (2 * a)
  y = (- b - Math.sqrt(d)) / (2 * a)
  if (x == y){ 
    
    console.log("Solution is " + x || y)
  }
};

  if (a !== 0 ) {
  
  d = b ** 2 - 4 * a * c
  
  x = (- b + Math.sqrt(d)) / (2 * a)
  y = (- b - Math.sqrt(d)) / (2 * a)
    if ( x != y ) {    
      
  console.log("Solution are " + x + " and " + y)
}

  };



  if (a === 0) {
  
  console.log("enter valid constats")
  
};
  
  
  var a = 0; b = 4; c = -5;

if (a !== 0 ) {
  
  d = b ** 2 - 4 * a * c
  
  x = (- b + Math.sqrt(d)) / (2 * a)
  y = (- b - Math.sqrt(d)) / (2 * a)
  if (x == y){ 
    
    console.log("Solution is " + x || y)
  }
};

  if (a !== 0 ) {
  
  d = b ** 2 - 4 * a * c
  
  x = (- b + Math.sqrt(d)) / (2 * a)
  y = (- b - Math.sqrt(d)) / (2 * a)
    if ( x != y ) {    
      
  console.log("Solution are " + x + " and " + y)
}

  };


  if (a === 0) {
  
  console.log("enter valid constats")
  
};
  



var a = 5; b = -13; c = 6;

if (a !== 0 ) {
  
  d = b ** 2 - 4 * a * c
  
  x = (- b + Math.sqrt(d)) / (2 * a)
  y = (- b - Math.sqrt(d)) / (2 * a)
  if (x == y){ 
    
    console.log("Solution is " + x || y)
  }
};

  if (a !== 0 ) {
  
  d = b ** 2 - 4 * a * c
  
  x = (- b + Math.sqrt(d)) / (2 * a)
  y = (- b - Math.sqrt(d)) / (2 * a)
    if ( x != y ) {    
      
  console.log("Solution are " + x + " and " + y)
}

  };


  if (a === 0) {
  
  console.log("enter valid constats")
  
};


var a = 3; b = -8; c = 12;

if (a !== 0 ) {
  
  d = b ** 2 - 4 * a * c
  
  x = (- b + Math.sqrt(d)) / (2 * a)
  y = (- b - Math.sqrt(d)) / (2 * a)
  if (x == y){ 
    
    console.log("Solution is " + x || y)
  }
};

  if (a !== 0 ) {
  
  d = b ** 2 - 4 * a * c
  
  x = (- b + Math.sqrt(d)) / (2 * a)
  y = (- b - Math.sqrt(d)) / (2 * a)
    if ( x != y &&  x !== Math.sqrt(x) || y !== Math.sqrt(y)) {    
      
  console.log("Solution are " + x + " and " + y)
}

  };



  if (a === 0) {
  
  console.log("enter valid constats")
  
};
  
  
  

  
  
  
  
  
